# Vibe Coding Rules

이 프로젝트에서는 아래 규칙을 기본 원칙으로 따른다.

## 1. 린트 규칙 준수

- 모든 코드는 `pyproject.toml`에 정의된 `ruff` 규칙을 따라야 한다.
- Python 코드는 VS Code에서 `Pyright` 언어 서버와 `python.analysis.typeCheckingMode = "standard"` 기준으로 문제 없이 읽히고 검사되도록 작성한다.
- 함수 시그니처, 반환값, 주요 지역 변수, 컨테이너 원소 타입이 드러나는 곳에는 타입 힌트를 명시해 `Pyright`의 `standard` 타입 체크에서 경고가 나지 않게 한다.
- `Any`, 암묵적 `None`, 느슨한 `dict`, 혼합 타입 컬렉션은 꼭 필요한 경우에만 사용하고, 가능하면 더 구체적인 타입으로 좁힌다.
- 옵셔널 값은 `None` 가능성을 분기에서 명확히 처리하고, 인덱싱/속성 접근 전에 타입이 좁혀졌는지 확인한다.
- import 정리는 `ruff` 기준으로 맞추고, 저장 시 `source.fixAll.ruff`, `source.organizeImports.ruff`가 적용된다는 전제로 불필요한 import, 정렬 어긋남, 자동 수정 가능한 린트 오류를 남기지 않는다.
- 기능 구현 후 끝내지 말고, 최소한 변경 범위에 대해 린트와 포맷을 확인한다.
- 작업 마무리 전 아래 명령으로 상태를 점검한다.

```bash
uv run ruff check .
uv run ruff format .
```

## 2. 테스트 먼저 작성하고 기능 구현

- 기능 코드를 먼저 작성하지 않는다.
- 먼저 실패하는 테스트를 만든 뒤, 그 테스트를 통과시키는 방식으로 구현한다.
- `.ipynb` 노트북에서 기능을 프로토타이핑할 때도 테스트성 검증 셀을 먼저 작성한다.
- 노트북 검증 셀은 가능한 한 `assert` 기반으로 작성하고, `Restart Kernel and Run All` 기준으로 재현 가능하게 통과해야 한다.
- 노트북에서 검증한 로직을 `src/catcher_llm`로 옮길 때는 해당 검증을 `tests/` 아래의 `pytest` 테스트로 다시 작성한다.
- 시각화, 탐색, 일회성 분석만 수행하는 노트북은 별도 테스트 파일 없이 진행할 수 있다.
- 구현 순서는 항상 아래 순서를 따른다.

```text
1. 테스트 또는 노트북 검증 셀 추가/수정
2. 테스트 또는 노트북 검증 셀이 실패하는지 확인
3. 기능 구현
4. 테스트 또는 노트북 검증 셀 통과 확인
5. `src/catcher_llm`로 반영하는 경우 `pytest` 테스트 추가 및 통과 확인
6. 린트 점검
```

- 테스트는 가능하면 `tests/` 아래의 관련 범위만 먼저 실행하고, 마무리 시 전체 테스트도 확인한다.

```bash
uv run pytest
```

## 기본 작업 원칙

- 새 기능, 버그 수정, 리팩터링 모두 동일하게 적용한다.
- 테스트 없이 넣는 구현은 예외 상황이 아니라면 허용하지 않는다. 노트북 프로토타입도 검증 셀 없이 구현하지 않는다.
- 재사용하거나 제품 코드로 반영할 로직은 노트북 검증에 머무르지 않고 반드시 `tests/`의 `pytest` 테스트로 승격한다.
- 린트 경고나 실패를 남긴 상태로 작업을 완료하지 않는다.

## 3. `.ipynb` 기능을 `src/catcher_llm`로 승격할 때의 모듈 규칙

- 노트북은 실험과 검증의 출발점일 뿐이며, 제품 코드에는 셀 단위 구현을 그대로 복사하지 않는다.
- 노트북에서 검증한 로직을 `src/catcher_llm`로 옮길 때는 먼저 노트북의 `assert` 검증을 `tests/` 아래 `pytest` 테스트로 승격한 뒤 구현한다.
- 제품 코드로 승격하는 로직은 책임에 맞는 모듈에 배치한다. 파일을 새로 만들기 전에 기존 모듈에 자연스럽게 들어갈 수 있는지 먼저 확인한다.

```text
- `schemas/`: 데이터 구조, 타입, 직렬화 규칙
- `prompts/`: 프롬프트 템플릿과 프롬프트 조립 함수
- `chains/`: prompt + model + parser 조합
- `services/`: 유스케이스 단위 오케스트레이션
- `retrievers/`: 검색, 로더, 벡터스토어 연동
- `db/`: DB 모델과 세션 관리
- `evaluation/`: 평가 데이터셋, 평가기, 실행기
- `utils/`: 여러 계층에서 재사용되는 순수 헬퍼 함수
```

- `src/catcher_llm`에 들어가는 함수와 클래스는 노트북 전용 상태에 의존하지 않게 작성한다. 전역 셀 변수, 실행 순서 의존성, 숨은 상태를 남기지 않는다.
- 제품 코드에는 `print`, `display`, 그래프 출력, 셀 출력 확인용 코드, 임시 디버깅 코드를 남기지 않는다.
- 하드코딩된 절대 경로, 노트북 파일 경로 기준 상대 경로, ad-hoc 환경변수 조회를 제품 코드에 직접 넣지 않는다. 설정이 필요하면 `config/settings.py`를 통해 주입한다.
- 데이터 가공이나 계산 로직은 가능한 한 순수 함수로 분리하고, 입출력과 오케스트레이션은 `services/`, `retrievers/`, `db/` 같은 경계 모듈에서 처리한다.
- 입출력 데이터 구조가 있으면 ad-hoc `dict`를 계속 전달하기보다 `schemas/`에 명시적 타입을 추가하는 쪽을 우선 검토한다.
- 기존 `src/catcher_llm` 공개 인터페이스와 맞지 않는 별도 스타일을 만들지 않는다. 이미 있는 네이밍, 타입 힌트, docstring, 반환 형태를 따른다.
- import 시점에 외부 호출이나 무거운 작업이 실행되지 않게 한다. 모듈 import만으로 API 호출, 파일 로드, DB 연결, 모델 초기화가 일어나지 않게 유지한다.
- 시각화, 탐색, 일회성 분석, 실험용 비교 코드는 계속 노트북에 남기고, 재사용 가능한 핵심 로직만 `src/catcher_llm`로 승격한다.
- 노트북에서 여러 단계를 한 셀에 묶어 구현했더라도 제품 코드로 옮길 때는 역할별 함수로 분리해 테스트 가능하게 정리한다.
- 새로운 제품 기능을 `src/catcher_llm`에 추가할 때는 아래 순서를 기본으로 따른다.

```text
1. 노트북 검증 셀의 핵심 시나리오를 `pytest` 테스트로 옮긴다.
2. 로직이 들어갈 패키지(`services`, `chains`, `schemas`, `utils` 등)를 결정한다.
3. 순수 계산 로직과 입출력/오케스트레이션 로직을 분리한다.
4. 필요한 타입, 헬퍼, 서비스 연결을 최소 범위로 구현한다.
5. 관련 테스트와 전체 테스트를 통과시킨다.
6. `ruff check`, `ruff format`으로 마무리한다.
```

## 4. 함수 설명 주석 규칙

- `tests/`, `src/`, `scripts/` 아래에서 새 함수를 만들 때는 해당 함수의 역할을 설명하는 한글 주석을 반드시 남긴다.
- 함수 설명은 가능하면 함수 정의 바로 아래의 한글 docstring으로 작성한다.
- 한 줄 docstring만으로 부족하면 함수 바로 위에 짧은 한글 주석을 추가해 의도와 맥락을 보강한다.
- 테스트 함수는 무엇을 검증하는지와 기대 결과를, 제품 함수는 책임과 입출력 의도를, 스크립트 함수는 실행 목적과 주요 부작용을 드러나게 작성한다.
- 이름만 읽어도 자명한 경우에도 설명을 생략하지 않는다. 이 프로젝트에서는 함수 설명을 문서화 규칙으로 간주한다.

## 5. 프롬프트 작성 언어 규칙

- `src/catcher_llm`와 노트북에서 작성하는 프롬프트 템플릿, 시스템 메시지, 사용자 메시지, 예시 입출력 문구는 기본적으로 한글로 작성한다.
- 외부 API, 모델, 라이브러리, 평가 데이터셋이 영어 프롬프트를 명시적으로 요구하는 경우에만 영어를 사용하고, 해당 이유를 코드나 노트북 맥락에서 확인 가능하게 남긴다.

## 6. 개발자 피드백의 규칙화

- 코드 생성, 수정, 리뷰 이후 개발자가 특정 실수나 누락을 지적하면 같은 실수가 반복되지 않도록 해당 내용을 AGENTS.md 규칙으로 승격할지 검토한다.
- 지적 내용이 이 프로젝트 전반에 반복 적용될 수 있는 코딩 방식, 테스트 방식, 문서화 방식, 프롬프트 작성 방식, 검증 절차에 해당하면 AGENTS.md에 명시한다.
- 규칙으로 추가할 때는 단순히 지적 내용을 복사하지 말고, 다음 작업자가 바로 적용할 수 있도록 금지할 행동, 권장할 행동, 적용 범위를 구체적으로 작성한다.
- 특정 일회성 상황, 임시 의사결정, 이미 해결된 개별 버그처럼 반복 적용 가치가 낮은 내용은 AGENTS.md에 추가하지 않는다.
- 새로 추가한 피드백 기반 규칙이 기존 규칙과 충돌하면 기존 규칙을 함께 정리해 하나의 일관된 기준으로 만든다.
